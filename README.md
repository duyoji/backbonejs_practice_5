```
# install mongodb from homebrew
$ brew update
$ brew install mongodb

# launch mongod with '--dbpath' option.
# Store data in /usr/local/var/mongodb instead of the default /data/db
$ mongod --dbpath /usr/local/var/mongodb
```